<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme — Compose handles all theming via MatruSnehTheme,
         this just prevents a flash of unstyled content on launch -->
    <style name="Theme.MatruSnehHealth" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
